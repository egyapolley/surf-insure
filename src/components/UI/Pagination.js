import React from 'react';
import _ from 'lodash'

function Pagination(props) {
    const {numOfPages,onClick,currentPage}=props

    return (
        <div>

        </div>
    );
}

export default Pagination;
